# Stage 4 변경사항 - 시연 예약 설정

이번 단계에서는 기존 고정 시연 예약(+1분 / +4분 / +7분)을 사용자가 직접 설정할 수 있도록 변경했습니다.

## 추가된 기능
- 시연 급식 시작 버튼을 누르면 `시연 급식 예약` 설정창 표시
- 1/2/3회차 각각 아래 항목 직접 설정
  - 현재 시각 기준 몇 분 후 실행할지
  - 사료량(g)
  - 약 A 개수
  - 약 B 개수
- 마지막 설정값 자동 저장 및 다음 실행 때 불러오기
- 기본값은 기존 흐름과 동일하게 +1분 / +4분 / +7분
- 급식 간 최소 2분 간격 검증
- 사료량 1~999g 검증
- 약 A/B 회차당 0~7개 검증
- 센서 패킷을 받은 상태라면 현재 남은 약 개수보다 많이 예약하지 못하도록 검증
- 시연 진행 중 시작 버튼을 다시 누르면 중지 확인창 표시
- ATmega의 `k` 명령이 토글 방식인 점을 반영하여 이미 자동 모드일 때 중복 전송하지 않음

## ATmega 변경 여부
- 변경 없음
- 기존 `k`, `wx...y...z...\n`, `j` 명령을 그대로 사용합니다.

## 변경 파일
- `app/src/main/java/com/example/dogfood/MainActivity.kt`
- `app/src/main/java/com/example/dogfood/Prefs.kt`
- `app/src/main/res/layout/dialog_demo_schedule.xml` (신규)
